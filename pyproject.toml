[project]
name = "kaliok-v2"
version = "0.1.0"
description = "kaliok V2"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "slow: tests lents avec OCR réel",
]
addopts = '-m "not slow"'
